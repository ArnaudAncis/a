while ( `\textit{condition}` ) {
  `\textit{statements}`
}

for ( `\textit{init}`; `\textit{condition}`; `\textit{increment}` ) {
  `\textit{statements}`
}

for ( `\textit{type}` `\textit{iterator-id}` : `\textit{collection}` ) {
  `\textit{statements}`
}

do {
  `\textit{statements}`
} while ( `\textit{condition}` );
